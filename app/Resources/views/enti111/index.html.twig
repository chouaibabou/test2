    {% extends 'admin.html.twig' %}

{% block body %}

    <section class="panel">
        <header class="panel-heading">
            <h1>Enti111s list</h1>
        </header>
        <div class="panel-body">
            <div class="adv-table editable-table">
                <div class="clearfix">
                                        <a href="{{ path('enti111_new') }}">
                        <div class="btn-group">
                            <button type="button" class="btn btn-primary"><a href="{{ path('enti111_new') }}">Create a newenti111</a> </button>
                        </div>
                    </a>
                                    </div>
                <br>
                    <table class="display table table-striped table-hover table-bordered">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Dks</th>
                                <th>Kdlsjq</th>
                                <th>Dlkslk</th>
                                <th>Actions</th>
                            </tr>
                        </thead>

                            <tfoot>
                        {% for enti111 in enti111s %}
                            <tr>
                                <td><a href="{{ path('enti111_show', { 'id': enti111.id }) }}">{{ enti111.id }}</a></td>
                                <td>{{ enti111.dks }}</td>
                                <td>{{ enti111.kdlsjq }}</td>
                                <td>{{ enti111.dlkslk }}</td>
                                <td>
                                    <ul>
                                        <li>
                                            <a href="{{ path('enti111_show', { 'id': enti111.id }) }}">show</a>
                                        </li>
                                        <li>
                                            <a href="{{ path('enti111_edit', { 'id': enti111.id }) }}">edit</a>
                                        </li>
                                    </ul>
                                </td>
                            </tr>
                        {% endfor %}
                            </tfoot>

                    </table>
                </div>
            </div>
    </section>
    <ul>
        <li>
                    </li>
    </ul>
{% endblock %}
